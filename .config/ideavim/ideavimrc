let mapleader=","

set hlsearch
set number relativenumber
set commentary
set highlightedyank
set multiple-cursors
set idearefactormode keep
set ideajoin true
set NERDTree

nnoremap Y y$

nmap ss <Action>(RecentFiles)
nmap gd <Action>(GotoDeclaration)
nmap gt <Action>(GotoTest)
nmap gr <Action>(FindUsages)
nmap gi <Action>(QuickImplementations)
nmap gv <Action>(opennInSplittedTab)
nmap [d <Action>(GotoNextError)
nmap ]d <Action>(GotoPrevError)
nmap K <Action>(QuickJavaDoc)
nnoremap <C-L> :nohls<cr><C-L>

nmap <leader><leader>w <Action>(AceWordAction)

nmap <C-o> <Action>(Back)
nmap <C-i> <Action>(Forward)
nmap <C-k> <Action>(ParameterInfo)
imap <C-k> <Action>(ParameterInfo)

nmap <C-e> <Action>(RecentFiles)
imap <C-e> <Action>(RecentFiles)

imap <M-a> <Action>(copilot.requestCompletions)
imap <M-j> <Action>(copilot.cycleNextInlays)
imap <M-k> <Action>(copilot.cyclePrevInlays)

imap <M-k> <Action>(ActivateGitHubCopilotChatToolWindow)

nmap <leader>rn <Action>(RenameElement)

xmap <leader>ff <Action>(ReformatCode)

nmap <leader>ss <Action>(SearchEverywhere)
nmap <leader>sf <Action>(GotoFile)
nmap <leader>sc <Action>(GotoClass)
nmap <leader>sa <Action>(GotoAction)

map <leader>tt <Action>(RunClass)

map <leader>a <Action>(Annotate)

nnoremap <C-n> :NERDTreeFocus<CR>

set ideastatusicon=gray